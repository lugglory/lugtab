# 요구사항 대조 기록

> 이 기록은 두 플러그인이 한 저장소의 `obsidian-plugins/` 아래에 함께 있던 시기에 작성되었습니다. 날짜가 붙은 검토 기록은 당시 상태 그대로 남깁니다.

## 사용자 요청

| 요청 | 반영 |
| --- | --- |
| `obsidian-plugin` 브랜치, Git/문서 이동 독립 폴더 | 두 플러그인 각각 번들·매니페스트, Git 스타일 파일 |
| 문서 이동 버튼 | 각 Markdown 제목줄의 이전/다음 버튼, 가로/세로 아이콘 |
| 끝에서 순환 | 위→아래, 왼쪽→오른쪽 순서. 이전은 역순 |
| 이동할 패널이 없으면 생성 | 텍스트는 다음 오른쪽·이전 왼쪽에 새 메모 생성. 탭 이동은 아래쪽에 생성. 전체 보내기 취소 시 생성 없음 |
| 확인창 이름/오른쪽 정렬/Enter·Space | 공통 확인창으로 통일, 승인 포커스, 취소/Esc/키 반복 처리 |
| 탭 제목 더블클릭 이동 | 다음, Shift는 이전. 대상 문서 확인 후 원래 탭을 닫으며 빈 원래 패널도 남기지 않음. 파일은 보존 |
| 포커스 전환 명령 | 기본 단축키 없음(가이드라인에 따라 사용자 지정), 선택 유지, 순환 |

## 기존 앱과 대조하며 보완한 항목

- 빈 대상 패널에 원본과 같은 폴더의 새 문서를 만들고 보내기.
- 대상 저장/내용 확인 전 원본을 제거하지 않기. 저장 실패·동시 편집·다중 선택·같은 파일 거부.

## 검증 범위와 차이

- UI는 실제 Electron/DOM과 Obsidian API 대역으로 검증합니다. **실제 Obsidian 호스트에서 검증했다는 뜻은 아닙니다.**
- 파일 저장·일반 Undo·복구 이력은 Obsidian에 맡깁니다. 기존 Electron 앱의 파일 저장 엔진과 복구 보관함은 플러그인 범위에 포함하지 않습니다.
- 탭 이동은 공개 API로 문서를 다시 엽니다. 커서/선택/스크롤은 복원하지만 Undo 이력은 옮기지 않습니다.
- 기존 앱의 보라색 삽입 위치 미리보기 막대는 아직 이식하지 않았습니다. 삽입 동작 자체는 대상 편집기의 마지막 커서 위치를 사용합니다.

## 2026-09-18 소스·실행 파일 대조

- 기준: 대화의 최신 수정 요청, 최초 플러그인 커밋 `0a1ae60`, 현재 `obsidian-plugins/` 작업 트리. 위 요구사항 표를 실제 소스·스타일·매니페스트와 대조했습니다.
- `0a1ae60` 이후 코드 차이는 요청한 목록 행 축소, 입력창 자동 높이, 커밋+Push·커밋 우선 배치, 탭 이동 시 아래 패널 생성 및 원래 탭 닫기에 해당합니다. 그 이전에 반영한 버튼·색상·diff·확인창·종료 경고·순환 이동 기능도 유지되어 있습니다. 현재 파일에서 요청과 무관한 삭제나 이전 구현으로의 복귀는 발견하지 못했습니다.
- 기존 `main.js`를 덮어쓰지 않고 `build.mjs`와 같은 옵션으로 메모리에만 빌드했습니다. 두 플러그인 모두 기존 번들과 생성 결과가 바이트 단위 및 SHA-256 비교에서 완전히 일치했습니다. 소스만 최신이고 실행용 번들이 과거 버전인 상태는 아닙니다.
- 전체 자동 테스트 35개 통과. 재빌드하지 않은 기존 번들로 Electron UI 검증 통과: 버튼 순서·섹션 버튼 유지·기본 투명 배경·아이콘 강조·상태 색상·입력창 확장/축소·패널 생성·탭 이동 후 원래 탭 닫기·확인창·diff·종료 취소.
- 이번 검토로 런타임 코드를 추가 수정하지 않았습니다. 최신 수정사항과 이 검토 기록을 함께 커밋합니다.
- 범위 제한: 별도 보관함에 복사한 설치본, 실제 Obsidian 호스트 및 기록되지 않은 과거 복사 작업은 이번 대조에 포함하지 않았습니다. 위에 기록한 기존 이식 범위의 차이도 그대로입니다.

## 지연 로딩된 탭 이동 보완

- 위 검토 이후 실제 사용에서 대상 탭 확인 실패가 보고되었습니다. 기존 테스트는 `setViewState` 직후 문서가 준비되는 경우만 가정했으며, 소스·번들 일치와 별개로 Obsidian의 지연 로딩을 기다리는 처리가 빠져 있었습니다.
- `setViewState` → `await workspace.revealLeaf(destination)` → 원본·대상 내용 확인 → 선택·스크롤 복원 → 원래 탭 닫기 순서로 수정했습니다. [공식 API](https://github.com/obsidianmd/obsidian-api/blob/master/obsidian.d.ts)의 `revealLeaf` 완료는 대상 뷰의 로딩 완료를 보장합니다.
- 이동 테스트 16개 및 Electron UI 대역에서 지연 로딩을 검증합니다. 로딩 중에는 원본 탭을 유지하고, 로딩 완료 후에만 닫으며, 로딩 실패·원본 편집·실제 내용 불일치에는 원본을 보존합니다. 실제 Obsidian에서의 재확인은 별도입니다.
